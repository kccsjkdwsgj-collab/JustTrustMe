name: Xray Reality with Bore Tunnel

on:
  workflow_dispatch:

jobs:
  run-server:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # 1. تنزيل Xray
      - name: Install Xray
        run: |
          sudo bash -c "$(curl -L https://github.com/XTLS/Xray-install/raw/main/install-release.sh)" @ install

      # 2. توليد المفاتيح و UUID
      - name: Generate Keys and UUID
        id: keys
        run: |
          UUID=$(xray uuid)
          xray x25519 > /tmp/keys.txt
          PRIVATE_KEY=$(grep -i "private" /tmp/keys.txt | awk '{print $NF}')
          PUBLIC_KEY=$(grep -i "public" /tmp/keys.txt | awk '{print $NF}')
          SHORT_ID=$(openssl rand -hex 8)
          
          echo "UUID=$UUID" >> $GITHUB_ENV
          echo "PRIVATE_KEY=$PRIVATE_KEY" >> $GITHUB_ENV
          echo "PUBLIC_KEY=$PUBLIC_KEY" >> $GITHUB_ENV
          echo "SHORT_ID=$SHORT_ID" >> $GITHUB_ENV
          
          echo "=================================================="
          echo "✅ تم توليد بيانات الاتصال بنجاح! (احفظهم عشان هتحتاجهم في Netmod):"
          echo "UUID: $UUID"
          echo "Public Key: $PUBLIC_KEY"
          echo "Short ID: $SHORT_ID"
          echo "SNI: ea.com"
          echo "Flow: xtls-rprx-vision"
          echo "=================================================="

      # 3. وضع إعدادات Xray
      - name: Setup Xray Config
        run: |
          sudo tee /usr/local/etc/xray/config.json > /dev/null << EOF
          {
            "inbounds": [{
              "port": 10000,
              "protocol": "vless",
              "settings": {
                "clients": [{"id": "${{ env.UUID }}", "flow": "xtls-rprx-vision"}],
                "decryption": "none"
              },
              "streamSettings": {
                "network": "tcp",
                "security": "reality",
                "realitySettings": {
                  "show": false,
                  "dest": "ea.com:443",
                  "xver": 0,
                  "serverNames": ["ea.com"],
                  "privateKey": "${{ env.PRIVATE_KEY }}",
                  "shortIds": ["${{ env.SHORT_ID }}"]
                }
              }
            }],
            "outbounds": [{"protocol": "freedom"}]
          }
          EOF

      # 4. تشغيل Xray
      - name: Start Xray
        run: |
          sudo nohup xray run -c /usr/local/etc/xray/config.json > /dev/null 2>&1 &
          sleep 3

      # 5. تشغيل Bore Tunnel واستخراج العنوان
      - name: Start Bore Tunnel
        run: |
          wget https://github.com/ekzhang/bore/releases/download/v0.5.1/bore-v0.5.1-x86_64-unknown-linux-musl.tar.gz
          tar xzf bore-v0.5.1-x86_64-unknown-linux-musl.tar.gz
          chmod +x bore
          
          nohup ./bore local 10000 --to bore.pub > bore.log 2>&1 &
          sleep 5
          
          # استخراج البورت من اللوج
          PORT=$(grep -o 'bore.pub:[0-9]*' bore.log | cut -d: -f2)
          echo "PORT=$PORT" >> $GITHUB_ENV
          
          echo "=================================================="
          echo "📋 عنوان السيرفر بتاعك (Bore Address):"
          echo "=================================================="
          cat bore.log
          echo "=================================================="

      # 6. طباعة الرابط الجاهز للنسخ
      - name: Print Ready-to-Use Link
        run: |
          VLESS_LINK="vless://${{ env.UUID }}@bore.pub:${{ env.PORT }}?type=tcp&security=reality&sni=ea.com&fp=chrome&pbk=${{ env.PUBLIC_KEY }}&sid=${{ env.SHORT_ID }}&flow=xtls-rprx-vision#GitHub-Action-Bore"
          echo "=================================================="
          echo "🔗 الرابط الجاهز (انسخه واعمله Import في NekoBox):"
          echo "$VLESS_LINK"
          echo "=================================================="
          echo "⚠️ لو الرابط ده مشتغلش، جرب تغير كلمة ea.com في الرابط لـ www.apple.com"
          echo "=================================================="

      # 7. إبقاء الـ Action شغال
      - name: Keep Alive
        run: |
          while true; do sleep 3600; done
